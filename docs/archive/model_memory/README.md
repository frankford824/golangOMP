# Model Memory Archive

ARCHIVE ONLY.
NOT SOURCE OF TRUTH.
DO NOT USE FOR NEW INTEGRATION OR CURRENT API DECISIONS.

This folder stores historical model-memory notes that were moved out of the repository root to reduce handoff drift.

If a memory note conflicts with the current repo truth, ignore the memory note and use:

1. `docs/V0_9_BACKEND_SOURCE_OF_TRUTH.md`
2. `docs/api/openapi.yaml`
3. `transport/http.go`
