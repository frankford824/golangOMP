# Current Performance Analysis Entry

This path no longer duplicates retired route names or page contracts.

Use `transport/http.go` and `docs/api/openapi.yaml` to identify current endpoints before running performance analysis. Store new measurements under `docs/iterations/` with the tested commit, environment, query plan, and timestamp.

The pre-V8 report is preserved at `docs/archive/legacy_specs/perf-analysis-2026-07-pre-v8.md` for evidence only.
