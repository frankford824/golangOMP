{
  "enabled": true,
  "max_length": 32,
  "default_template": "{name}-{i_id}",
  "scene_templates": {
    "new_product_development": "{name}-{i_id}",
    "purchase_task": "{name}-{i_id}",
    "original_product_update": "{name}-{i_id}",
    "item_style_update": "{name}-{i_id}"
  },
  "templates": {
    "default": "{name}-{i_id}"
  }
}
