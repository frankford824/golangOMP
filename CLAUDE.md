# CLAUDE.md

Read root `AGENTS.md` at the start of each Claude Code session. It is the
canonical repository workflow; this file adds no independent rules.

Follow its "Session Start", "Reading Order", "Instruction Scope And
Authorization", "After Editing" and response-format sections. Read nested
`AGENTS.md` files for directories touched by the task. Confirm current state
from the repository rather than assuming an earlier session is still current.

The standing task entry prompt is `prompts/CODEX_SESSION_BOOTSTRAP.md`.
